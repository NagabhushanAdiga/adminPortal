# Server
PORT=3000

# MongoDB (required)
# Get connection string from MongoDB Atlas: Database → Connect → Drivers
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
DB_NAME=adminportal
